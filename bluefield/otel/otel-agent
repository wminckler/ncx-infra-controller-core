#!/usr/bin/env bash
set -euo pipefail

# forge-dpu-otel-agent wrapper that first copies mTLS certs from forge-dpu-agent.

/usr/local/sbin/otel_copy_certs.sh
/usr/bin/forge-dpu-otel-agent --config-path=/etc/forge-dpu-otel-agent/config.toml run
